[package]
name = "xdl"
version = "0.1.7"
authors = ["toyboot4e <toyboot4e@gmail.com>"]
edition = "2018"
license = "MIT"
description = "Extentional input layer on Rust-SDL2 or Rokol"
repository = "https://github.com/toyboot4e/xdl"

[features]
default = []

# please select your backend with feature flag
use-sdl2 = ["sdl2"]
use-rokol = ["rokol"]

[dependencies]
num_enum = "0.5.1"

sdl2 = { version = "^0.34.3", optional = true }
# rokol = { version = "^0.1.1", optional = true }
rokol = { path = "../rokol", optional = true }

